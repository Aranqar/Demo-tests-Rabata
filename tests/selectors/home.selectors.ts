export enum homePageSelectors {
  privacyModal = '#modalPrivacy',
  totalDataStoredText = '#tbApiStored',
  monthlyLoadedDataText = '#tbDownload',
  yearlyRabataChartPrice = '#rabataMobileApi',
  monthlyDataLoadInput = '#dataDownloadInput',
  dataStoredInput = '#dataApiStoredInput',
  mainTryItForFreeBtn = '.arrow-wrap .btn',
}
